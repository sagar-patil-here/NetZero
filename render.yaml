services:
  - type: web
    name: netzero-backend
    env: node
    rootDir: backend
    buildCommand: rm -rf node_modules && npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

